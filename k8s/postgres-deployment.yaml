apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: finlife
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16
        env:
        - name: POSTGRES_PASSWORD
          value: "finlife123"
        - name: POSTGRES_DB
          value: "finlife_users"
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: finlife
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432