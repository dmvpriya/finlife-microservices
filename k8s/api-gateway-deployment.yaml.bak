apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: finlife
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: finlife/api-gateway:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: finlife
spec:
  selector:
    app: api-gateway
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080